{# TEMPLATE: DEFAULT PAGE #}

{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper test">

		
			<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
		
			<section class="article-content bg-cream">
				<h1 class="bg-primary article-h1 font-bold text-4xl p-[3rem] uppercase flex justify-center text-yellow">{{post.title}}</h1>
				
				<div class="article-body grid grid-cols-3 gap-4 px-60 py-[6rem] *:bg-white *:flex *:justify-center relative *:p-14 text-sm">
					
					{{post.content}}
					
				</div>
			</section>
		</article>
	</div>

	<div
		class="debug p-8 bg-debug">
		{# useful for debugging, delete later #}
		<h1>Debug info</h1>

		<p class="pb-4">post name:
			{{post.name}}</p>

		<p>file name: page.twig</p>

		{# Output what is in the post #}
		{# {{dump(post)}} #}
	</div>

	<!-- /content-wrapper -->
{% endblock %}
