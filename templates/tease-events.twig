<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Page Title</title>
    <style>
        body {
            background-color: black;
            color: white; /* Optional: Set text color to white for better readability */
        }
    </style>
</head>
<body>
    <!-- Your HTML content goes here -->
    <article class="w-2/6 px-5 mb-5 tease tease-{{post.type}} bg-black" id="tease-{{post.id}}">
        {% block content %}
        <div class="bg-yellow-300 border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
            <img class="rounded-t-lg bg-cover bg-center h-40 w-full object-cover" src="{{ post.thumbnail.src }}" alt="" />
            <div class="p-5">
                <a href="{{post.link}}">
                    <h5 class="mb-2 text-2xl font-bold tracking-tight text-white dark:text-white">{{post.title}}</h5>
                </a>

                <a href="{{post.link}}" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-yellow-600 rounded-lg hover:bg-yellow-700 focus:ring-4 focus:outline-none focus:ring-yellow-300 dark:bg-yellow-600 dark:hover:bg-yellow-700 dark:focus:ring-yellow-800">
                    Read more
                    <svg class="rtl:rotate-180 w-3.5 h-3.5 ms-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
                    </svg>
                </a>
            </div>
        </div>
        {% endblock %}
    </article>
</body>
</html>
